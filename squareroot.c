#include<stdio.h>
#include<math.h>
int main()
{
	float n,result;
	printf("enter the value of n");
	scanf("%f",&n);
	result=sqrt(n);
	printf("square root of n is %f",result,n);
	return 0;
}
