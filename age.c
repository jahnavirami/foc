#include<stdio.h>
int main()
{
	int n,age;
	printf("enter the value of n");
	scanf("%d",&n);
	if(age<18)
	printf("not eligible to vote");
	else
	if(age>=18)
	printf("eligible to vote");
}

